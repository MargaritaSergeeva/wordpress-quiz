# Проверка основы проекта

Дата: 2026-09-17.

- SSH-доступ к GitHub и подключение `origin` проверены.
- Репозиторий публичный.
- `docker compose config --quiet` — успешно.
- `php -l scripts/bootstrap.php` — успешно.
- `php -l wp-content/plugins/wordpress-quiz/wordpress-quiz.php` — успешно.
- `.env` исключён из Git через `.gitignore`.

После восстановления доступа к Docker Hub локальная установка проверена:

- Контейнеры WordPress и MariaDB запущены, оба имеют статус healthy.
- WordPress установлен через WP-CLI; исправлен вызов команды (`wp eval-file`).
- Повторный `make install` успешно обнаруживает существующую установку.
- Каркас плагина `wordpress-quiz` версии 0.1.0 активен.
- `make lint` — успешно в контейнерах PHP.
- Главная страница и форма входа отвечают HTTP 200.
- Вход администратором проверен: получена авторизационная cookie,
  переход в `/wp-admin/`, HTTP 200. Пароль в журнал проверки не выводился.

Это проверка каркаса проекта, а не результаты тестирования готового квиза.
